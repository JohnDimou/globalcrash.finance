# pinned to the versions the index is validated against — pandas 3.x changes index/resample
# semantics and broke the composite on the first CI run (2026-09-02)
pandas==2.3.3
numpy==2.0.2
xlrd==2.0.2
openpyxl==3.1.5
lxml==6.1.1
Pillow==11.3.0
